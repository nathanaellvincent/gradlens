"""Ingestion — pulls job descriptions from upstream boards into local storage.

Currently Greenhouse-only. Ashby and Lever adapters follow the same shape
(fetch → parse → upsert) and will land in a later commit.
"""
